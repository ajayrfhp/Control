import torch

class ConvModelDataset(torch.utis.data.Dataset):
    def __init__(self,  window_size = 5, minutes_per_season_threshold = 1000, minutes_per_game_threshold = 30, num_features = 7):
        pass 

    def __len__(self):
        pass

    def __get__item(self, index):
        pass